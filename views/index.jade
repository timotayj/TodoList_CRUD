extends layout

block content

    p test
        ul

            each k, i in Todos
                if(k.isDone == false)
                    li
                    =k.content
                    =k._id
                    = k.isDone
                    form(action ='/', method ='PUT')
                    input#completed(type = 'hidden', name ='completed', clicked=('#{Todos.isDone}' === 'true'))
                    button(type='submit', ).
                            Done!




    p test

    form(action = '/', method = 'post')
        input.form-control(type = 'text', name = 'newContent', placeholder='Enter new task')

        button(type = 'submit').
            Submit






